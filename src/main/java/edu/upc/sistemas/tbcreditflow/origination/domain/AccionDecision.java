package edu.upc.sistemas.tbcreditflow.origination.domain;

/** Acción del comité en la decisión (HU08): body {@code { "accion": "APROBAR" | "RECHAZAR" }}. */
public enum AccionDecision {
    APROBAR,
    RECHAZAR
}
